const Node = require('./node');

class LinkedList {
    constructor (name) {
        this.name = name; 
    }

    append(data) {
        var a = new Node(data)
        append(node) {
		if (this.tail) {
		this.tail.nextNode = node;	
		} else {
		this.tail = node;
		}
		} 
    }

    head() {
        
    }

    tail() {
        
    }

    at(index) {
        
    }

    insertAt(index, data) {
        
    }
    isEmpty() {
        
    }

    clear() {

        
    }

    deleteAt(index) {

        
    }

    reverse() {
        
    }

    indexOf(data) {
        
    }
}

module.exports = LinkedList;
